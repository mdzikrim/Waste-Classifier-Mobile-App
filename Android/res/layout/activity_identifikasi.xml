<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FAFAFA">

    <!-- Panah + Text -->
    <TextView
        android:id="@+id/tv_touch_to_identify"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Touch to Identify"
        android:textSize="18sp"
        android:textColor="#575757"
        android:fontFamily="sans-serif-medium"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="60dp"/>

    <!-- FAB Kamera Besar -->

    <ImageView
        android:id="@+id/arrow_curved"
        android:layout_width="70dp"
        android:layout_height="60dp"
        android:src="@drawable/ic_curved_arrow"
        app:layout_constraintBottom_toTopOf="@id/btnIdentify"
        app:layout_constraintEnd_toStartOf="@id/btnIdentify"
        android:layout_marginEnd="8dp"
        android:layout_marginBottom="8dp"/>

    <androidx.cardview.widget.CardView
        android:id="@+id/btnIdentify"
        android:layout_width="110dp"
        android:layout_height="110dp"
        app:cardCornerRadius="55dp"
        app:cardElevation="6dp"
        app:layout_constraintTop_toBottomOf="@id/tv_touch_to_identify"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="44dp">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/green">

            <ImageView
                android:id="@+id/icCamera"
                android:layout_width="70dp"
                android:layout_height="70dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_camera_logo"
                app:tint="#fff"/>
        </FrameLayout>
    </androidx.cardview.widget.CardView>

    <!-- FAB Galeri Kecil -->
    <androidx.cardview.widget.CardView
        android:id="@+id/btnGaleri"
        android:layout_width="56dp"
        android:layout_height="56dp"
        app:cardCornerRadius="28dp"
        app:cardElevation="4dp"
        app:layout_constraintBottom_toBottomOf="@id/btnIdentify"
        app:layout_constraintEnd_toStartOf="@id/btnIdentify"
        android:layout_marginEnd="30dp"
        android:layout_marginBottom="10dp">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/white">

            <ImageView
                android:id="@+id/icGaleri"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_galeri"
                app:tint="@color/green"/>
        </FrameLayout>
    </androidx.cardview.widget.CardView>

    <!-- Label Galeri -->
    <TextView
        android:id="@+id/tvGaleri"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Galeri"
        android:textColor="#888"
        android:textSize="14sp"
        app:layout_constraintTop_toBottomOf="@id/btnGaleri"
        app:layout_constraintStart_toStartOf="@id/btnGaleri"
        android:layout_marginTop="2dp"
        android:layout_marginStart="8dp"/>

    <!-- Bottom Navigation -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigationView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="#FFFFFF"
        app:menu="@menu/bottom_nav_menu"
        app:itemIconTint="@color/nav_item_color"
        app:itemTextColor="@color/nav_item_color"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
